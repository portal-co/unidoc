# unidoc

Universal documentation system used by Portal Solutions.

**Status: scaffold only. No crates or packages have been added yet.**

## Repository layout

```
unidoc/
├── Cargo.toml       # Rust virtual workspace (no members)
├── package.json     # npm workspace root (no members)
├── crates/          # empty — placeholder for future Rust crates
└── packages/        # empty — placeholder for future npm packages
```

## What exists

### Rust (`Cargo.toml`)

A virtual workspace using Cargo resolver v3 (Rust edition 2024 resolver semantics). The `members` array is empty. Running `cargo build` or `cargo check` against this manifest produces:

```
error: manifest path contains no package: The manifest is virtual, and the workspace has no members.
```

No crates, no `src/` directories, no `.rs` files.

### Node.js (`package.json`)

An ESM package (`"type": "module"`) named `@portal-solutions/unidoc`. The `workspaces` array is empty. One dev dependency is declared:

- [`zshy`](https://www.npmjs.com/package/zshy) `^0.7.0` — a zsh-based shell scripting helper

No source files, no scripts, no entry point.

## Git history

| Commit | Message |
|--------|---------|
| `bb25f01` | Initial scaffold |

One commit. The repository was generated by tooling (`portal-hot`) and has not had application code added yet.

## Development environment

The `.rustc_info.json` in `target/` records the last toolchain used:

- rustc 1.92.0 (`ded5c06cf`, 2025-12-08)
- Host: `aarch64-apple-darwin`
- LLVM 21.1.3

## Origin

`git@github.com:portal-co/unidoc.git`
